#include <bits/stdc++.h>
using namespace std;
int main(){
    cout<<"enter the no. of elements"<<endl;
    int n;
    cin>>n;
  multiset<int>ms;
  cout<<"Enter the elements"<<endl;
    for(int i=0;i<n;i++){
        int x=0;
        cin>>x;
       ms.insert(x);
    }
    for( auto it:ms){
        cout<<it<<" ";
    }
    
    return 0;
}
